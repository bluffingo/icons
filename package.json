{
    "devDependencies": {
        "gulp-svg-sprite": "^2.0.3",
        "svg-sprite": "^2.0.4"
    },
    "scripts": {
        "build:sprites": "svg-sprite --config svg-sprite.config.json icons/*.svg"
    }
}
